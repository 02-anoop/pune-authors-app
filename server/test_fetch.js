const axios = require('axios');
const jwt = require('jsonwebtoken');

async function test() {
  try {
    // We need an admin token
    // Actually, I can just write a quick express route or use prisma to dump the exact mapped output.
  } catch (e) {
    console.error(e);
  }
}
test();
